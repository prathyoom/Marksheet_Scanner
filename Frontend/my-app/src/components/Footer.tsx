const Footer = ({ color }: { color: string }) => (
  <p className={`text-center text-xs text-${color} absolute -mt-8 z-10 w-full`}>
    Prathyoom MS. All Rights Reserved.
  </p>
);

export default Footer;
